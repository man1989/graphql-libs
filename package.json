{
  "name": "graphql-libs-monorepo",
  "version": "0.1.0",
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "author": "",
  "license": "ISC",
  "description": "Common library for building domain subgraph",
  "workspaces": [
    "packages/*"
  ],
  "volta": {
    "node": "20.17.0"
  },
  "dependencies": {
    "@graphql-tools/schema": "^10.0.6"
  },
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
